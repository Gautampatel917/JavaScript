"use strict" //treat all javascript code as newer version

const accountId = "gautam"; //lock the variable.
let emailId = "demo@gamil.com"; //scope oriented
var password = 1204;
cityName = "mumbai";

/*
    prefer to not use var
    because of issue in block scope and functional scope
*/

console.table([accountId, emailId, password, cityName]);